# pretty_awesome_camera — archived

This repository was folded into the **Waffles monorepo** on 2026-08-24 and is now a read-only archive.

**Current home:** https://github.com/Wednesday-Waffles/Waffles → `packages/pretty_awesome_camera/`

History was preserved in the monorepo via `git filter-repo`, so `git log` and `git blame` work on these files at their new paths. This archive remains as the historical record of the standalone repository.

See `docs/plans/active/waffles-monorepo-convergence.md` in the monorepo for the migration plan and audit trail.
